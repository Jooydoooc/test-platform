node_modules
.next
out
.DS_Store
*.log
.env*
!.env.example
.playwright-mcp/
.vercel
*.tsbuildinfo

.claude/

# Review-tool critique artifacts — local output, not part of the app
.impeccable/

# Supabase DB password (local record only — never commit)
.supabase-db-password.txt

# Source material for content imports — not part of the app
*.pdf
